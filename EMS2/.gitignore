# Composer dependencies
vendor/
composer.phar

# Uploaded files (photos, etc.) — keep folder structure but not files
uploads/id_photos/*
uploads/advisers/*
!uploads/id_photos/.gitkeep
!uploads/advisers/.gitkeep

# Keep templates folder tracked
!uploads/templates/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/settings.json

# PHP error logs
*.log
error_log
